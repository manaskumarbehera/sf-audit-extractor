# NPM Configuration for Salesforce CLI Installation
# This configuration is used when running: npm install --global @salesforce/cli

# Use the default npm registry
registry=https://registry.npmjs.org/

# Set global installation prefix (optional - uncomment to customize)
# prefix=/usr/local

# Enable progress bar during installation
progress=true

# Set log level for npm operations
loglevel=info

# Timeout settings for slow connections
fetch-timeout=60000
fetch-retries=3

# Save exact versions
save-exact=true

# Engine strict mode - ensures Node.js version compatibility
engine-strict=true

