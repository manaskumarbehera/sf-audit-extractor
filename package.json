{
  "name": "sf-audit-extractor",
  "version": "1.0.0",
  "description": "Salesforce SOQL Builder / Audit Extractor Chrome Extension",
  "main": "background.js",
  "scripts": {
    "test": "jest --runInBand --detectOpenHandles",
    "lint": "eslint . --ext .js",
    "start": "echo 'This is a Chrome extension project — load via chrome://extensions'",
    "build:ext": "rm -rf dist && mkdir -p dist && rsync -av --exclude='__tests__' --exclude='test_*.js' --exclude='test-*.js' --exclude='*.spec.js' --exclude='*.test.js' --exclude='.git' --exclude='.idea' --exclude='node_modules' --exclude='test-results' ./ dist/"
  },
  "keywords": [
    "salesforce",
    "soql",
    "chrome-extension",
    "jest",
    "testing"
  ],
  "author": "Manas Kumar Behera",
  "license": "MIT",
  "type": "module",
  "dependencies": {},
  "devDependencies": {
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0",
    "eslint": "^9.13.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {
      "^.+\\.jsx?$": "babel-jest"
    },
    "transformIgnorePatterns": [
      "/node_modules/"
    ],
    "moduleFileExtensions": [
      "js",
      "json"
    ],
    "testMatch": [
      "**/__tests__/run_existing_scripts.test.js",
      "**/__tests__/**/*.spec.js"
    ],
    "verbose": true
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
